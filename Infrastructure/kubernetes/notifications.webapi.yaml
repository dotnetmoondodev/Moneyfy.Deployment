apiVersion: apps/v1
kind: Deployment
metadata:
  name: notifications-webapi-deployment
spec:
  selector:
    matchLabels:
      app: notifications-webapi
  template:
    metadata:
      labels:
        app: notifications-webapi
    spec:
      containers:
        - name: notifications-webapi
          image: moneyfy.azurecr.io/notifications.moneyfy.webapi:latest
          env:
            - name: Authority
              valueFrom:
                secretKeyRef:
                  name: notifications-webapi-secrets
                  key: authority
            - name: Audience
              valueFrom:
                secretKeyRef:
                  name: notifications-webapi-secrets
                  key: audience
            - name: DBConnection
              valueFrom:
                secretKeyRef:
                  name: notifications-webapi-secrets
                  key: dbconnection
          resources:
            limits:
              memory: "128Mi"
              cpu: "250m"
            requests:
              memory: "64Mi"
              cpu: "150m"
          ports:
            - containerPort: 8030
