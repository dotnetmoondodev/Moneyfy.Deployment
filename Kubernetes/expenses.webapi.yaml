apiVersion: apps/v1
kind: Deployment
metadata:
  name: expenses-webapi-deployment
spec:
  selector:
    matchLabels:
      app: expenses-webapi
  template:
    metadata:
      labels:
        app: expenses-webapi
    spec:
      containers:
        - name: expenses-webapi
          image: moneyfy.azurecr.io/expenses.moneyfy.webapi:latest
          env:
            - name: Authority
              valueFrom:
                secretKeyRef:
                  name: expenses-webapi-secrets
                  key: authority
            - name: Audience
              valueFrom:
                secretKeyRef:
                  name: expenses-webapi-secrets
                  key: audience
            - name: DBConnection
              valueFrom:
                secretKeyRef:
                  name: expenses-webapi-secrets
                  key: dbconnection
          resources:
            limits:
              memory: "128Mi"
              cpu: "250m"
            requests:
              memory: "64Mi"
              cpu: "150m"
          ports:
            - containerPort: 8027
